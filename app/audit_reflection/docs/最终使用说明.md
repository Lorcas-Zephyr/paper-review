# 反思评估组 - 最终使用说明

## ✅ 项目已完成并可用

所有功能已实现，代码已清理，可以直接投入使用。

## 快速开始（3步）

### 步骤1: 生成测试数据
```bash
python tests/generate_test_data.py --num-papers 3
```

**输出**:
- 在`prompts/`文件夹生成15个JSON文件（3篇论文 × 5个审计组）
- 文件命名: `test_paper_001_group_2.json` 到 `test_paper_003_group_6.json`

### 步骤2: 运行评审程序
```bash
python run.py
```

**交互式选择**:
```
请选择运行方案:
1. 从PostgreSQL数据库读取 (agent_audits表)
2. 从prompts文件夹读取JSON文件
0. 退出

请输入选项 (0-2): 2
请输入JSON文件目录 (默认: prompts): [直接回车]
```

**或使用命令行参数**:
```bash
# 从文件读取
python run.py --mode file --prompts-dir prompts

# 从数据库读取
python run.py --mode database

# 处理指定论文
python run.py --mode database --paper-id paper_001
```

### 步骤3: 查看评审报告
```bash
# 查看生成的Markdown报告
ls reports/
# 输出: review_report_test_paper_001_20260305_143022.md

# 查看JSON结果
ls prompts/result_*.json
# 输出: result_test_paper_001.json
```

## 输出说明

### 1. Markdown评审报告 (reports/)

每篇论文生成一个完整的Markdown报告，包含：

```markdown
# 硕士学位论文评审报告

## 一、执行摘要
- 综合评分: 82.5 / 100
- 评审等级: 良好 (B)
- 问题统计: Critical 0个, Major 3个, Minor 5个

## 二、各审计组评审详情
- 评分分布
- 审计组权重说明

## 三、冲突裁决与加权投票
- 冲突检测机制
- 本次评审冲突情况
- 冲突裁决详情
- 加权投票机制

## 四、证据真实性验证（幻觉过滤）
- 验证机制
- 验证结果
- 无效证据详情
- 强制证据关联

## 五、问题详细列表
- 关键问题 (Critical) ⚠️
- 重要问题 (Major) ⚡
- 次要问题 (Minor) ℹ️

## 六、导师指导意见
- 指导对话内容

## 七、人工复核建议
- 是否需要人工复核
- 触发原因

## 八、最终评审建议
- 综合评分
- 评审结论
- 具体建议
```

### 2. JSON结果文件 (prompts/)

```json
{
  "paper_id": "test_paper_001",
  "final_score": 82.5,
  "verdict": "论文质量良好，建议小修后录用",
  "critical_issues": [],
  "major_issues": [...],
  "minor_issues": [...],
  "needs_human_review": false,
  "human_review_reason": null,
  "mentor_dialogue": {...},
  "plugin_metadata": {
    "paper_title": "Paper_test_paper_001",
    "markdown_report_path": "reports/review_report_test_paper_001_20260305_143022.md",
    "conflict_resolution": {...},
    "review_marks_count": 0,
    "sorted_results_count": 15
  }
}
```

## 核心功能验证

### ✅ 任务A: 冲突裁决逻辑
- **位置**: Markdown报告第三章
- **内容**:
  - 冲突检测结果
  - 加权投票过程
  - LLM裁决意见
  - 最终裁决结论

### ✅ 任务B: 幻觉过滤检测
- **位置**: Markdown报告第四章
- **内容**:
  - 证据验证统计
  - 有效/无效证据数量
  - 验证通过率
  - 剔除的无证据问题

### ✅ 任务C: Markdown报告模版
- **位置**: `reports/` 文件夹
- **特点**:
  - 8个主要章节
  - 导师友好格式
  - 完整评审流程
  - 明确的建议

### ✅ 强制证据关联
- Warning/Critical级别问题必须有evidence_quote
- 无证据问题自动剔除
- 统计记录在报告第四章

## 数据库模式（可选）

如果使用数据库模式，需要以下配置：

### 数据库连接
- 主机: 127.0.0.1:5432
- 用户名: admin
- 密码: <通过环境变量设置>
- 数据库: paper_review

### 表结构

#### agent_audits表
```sql
CREATE TABLE agent_audits (
    paper_id VARCHAR(255),
    group_id INT,  -- 2-6 (格式、逻辑、代码、实验、文献)
    result_json JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### paper_sections表（用于证据验证）
```sql
CREATE TABLE paper_sections (
    section_id VARCHAR(255),
    paper_id VARCHAR(255),
    section_name VARCHAR(255),
    section_content TEXT,
    content_vector VECTOR  -- 可选，用于语义搜索
);
```

#### reflection_results表
```sql
CREATE TABLE reflection_results (
    paper_id VARCHAR(255) PRIMARY KEY,
    final_score FLOAT,
    verdict TEXT,
    result_json JSONB,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

## 环境配置

### 必需配置
```bash
# DeepSeek API密钥（必需）
export DEEPSEEK_API_KEY="your_api_key_here"

# Windows
set DEEPSEEK_API_KEY=your_api_key_here
```

### 可选配置
如果需要修改数据库配置，编辑 `src/db/database.py`:
```python
DB_CONFIG = {
    "host": "127.0.0.1",
    "port": 5432,
    "user": "admin",
    "password": "<通过环境变量设置>",
    "database": "paper_review"
}
```

## 常见问题

### Q1: 测试数据生成失败？
```bash
# 确保在项目根目录
cd project/
python tests/generate_test_data.py --num-papers 3
```

### Q2: 运行时找不到模块？
```bash
# 确保在项目根目录运行
cd project/
python run.py
```

### Q3: DeepSeek API调用失败？
```bash
# 检查环境变量
echo $DEEPSEEK_API_KEY  # Linux/Mac
echo %DEEPSEEK_API_KEY%  # Windows

# 如果未设置，设置环境变量
export DEEPSEEK_API_KEY="your_key"
```

### Q4: 数据库连接失败？
- 检查网络是否能访问 127.0.0.1
- 确认数据库名称是否正确
- 如需修改，编辑 `src/db/database.py`

### Q5: 没有生成Markdown报告？
- 检查 `reports/` 文件夹是否存在
- 查看程序日志中的错误信息
- 确认有写入权限

## 项目结构

```
project/
├── run.py                          # ⭐ 主运行程序
├── reports/                        # ⭐ Markdown报告输出
├── prompts/                        # JSON文件输入/输出
├── src/
│   ├── db/                         # 统一数据库模块
│   ├── api/                        # 统一API模块（DeepSeek）
│   ├── common/                     # 公共模块
│   ├── conflict_resolution/        # 成员A：冲突裁决
│   ├── deduplication/              # 成员B：重复过滤
│   ├── evidence_validation/        # 成员B：证据验证
│   ├── dialogue_generation/        # 成员C：对话生成
│   └── priority_sorting/           # 成员D：优先级排序
├── tests/
│   └── generate_test_data.py       # 测试数据生成器
└── docs/
    ├── 快速启动指南.md
    ├── 功能验证清单.md
    ├── 功能覆盖验证报告.md
    └── ...
```

## 验证清单

使用前请确认：

- [ ] Python 3.8+ 已安装
- [ ] 已安装依赖: `pip install -r requirements.txt`
- [ ] 已设置 DEEPSEEK_API_KEY 环境变量
- [ ] 在项目根目录运行命令
- [ ] `reports/` 文件夹存在（自动创建）
- [ ] `prompts/` 文件夹存在（自动创建）

## 下一步

1. **测试运行**: 使用测试数据验证功能
2. **查看报告**: 打开Markdown报告了解评审流程
3. **连接数据库**: 配置真实数据库进行生产使用
4. **自定义配置**: 根据需求调整审计组权重等参数

## 技术支持

如遇问题，请查看：
1. 程序日志输出（详细错误信息）
2. `docs/功能验证清单.md` - 功能验证方法
3. `docs/功能覆盖验证报告.md` - 完整功能说明
4. `docs/快速启动指南.md` - 详细使用指南

---

**项目已完成，可以直接使用！** 🎉
