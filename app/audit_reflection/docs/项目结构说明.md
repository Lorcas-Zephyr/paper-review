# 反思评估组 - 重构后项目结构

## 项目概述

本项目是反思评估组的完整实现，采用模块化设计，支持从数据库或文件读取审计结果，进行冲突裁决、证据验证、优先级排序和导师对话生成。

## 项目结构

```
project/
├── src/                                    # 源代码目录
│   ├── common/                             # 公共模块
│   │   ├── models.py                       # 统一数据模型
│   │   ├── config_b.py                     # 成员B配置
│   │   ├── config_c.py                     # 成员C配置
│   │   ├── utils_b.py                      # 成员B工具函数
│   │   ├── database_a.py                   # 成员A数据库（旧）
│   │   ├── database_b.py                   # 成员B数据库（旧）
│   │   └── database_c.py                   # 成员C数据库（旧）
│   │
│   ├── db/                                 # 统一数据库模块
│   │   ├── __init__.py
│   │   └── database.py                     # 统一数据库管理器
│   │                                       # 连接: 127.0.0.1, 用户: admin, 密码: <通过环境变量设置>
│   │
│   ├── api/                                # 统一API模块
│   │   ├── __init__.py
│   │   └── deepseek_client.py              # DeepSeek API客户端
│   │
│   ├── conflict_resolution/                # 成员A：冲突裁决模块
│   │   ├── __init__.py
│   │   ├── conflict_resolver.py            # 冲突裁决核心逻辑
│   │   └── llm_client.py                   # LLM客户端（旧，已被api/deepseek_client替代）
│   │
│   ├── deduplication/                      # 成员B：重复过滤模块
│   │   ├── __init__.py
│   │   └── dedup.py                        # DBSCAN聚类去重
│   │
│   ├── evidence_validation/                # 成员B：证据验证模块
│   │   ├── __init__.py
│   │   └── evidence.py                     # 精确/语义匹配验证
│   │
│   ├── dialogue_generation/                # 成员C：导师对话生成模块
│   │   ├── __init__.py
│   │   └── dialogue_engine.py              # 对话生成引擎
│   │
│   ├── priority_sorting/                   # 成员D：优先级排序模块
│   │   ├── __init__.py
│   │   └── review_engine.py                # 规则引擎排序和复核标记
│   │
│   └── main.py                             # 旧的主入口（已被run.py替代）
│
├── tests/                                  # 测试目录
│   ├── generate_test_data.py              # 测试数据生成器
│   ├── test_conflict_resolver.py          # 冲突裁决测试
│   ├── test_review_engine.py              # 复核引擎测试
│   └── conftest_a.py                       # 测试配置
│
├── prompts/                                # 提示词和JSON文件目录
│   ├── conflict_resolution/                # 冲突裁决提示词模板
│   └── *.json                              # 测试用JSON文件（由generate_test_data.py生成）
│
├── docs/                                   # 文档目录
│   ├── full_flow_guide.txt                 # 完整流程指南
│   ├── work_week2.txt                      # 第二周工作说明
│   ├── 合并说明.md                          # 代码合并说明
│   └── 项目结构说明.md                      # 本文件
│
├── member_a/                               # 成员A原始代码（保留）
├── member_b/                               # 成员B原始代码（保留）
├── member_c/                               # 成员C原始代码（保留）
├── member_d/                               # 成员D原始代码（保留）
│
├── run.py                                  # 主运行程序（新）
├── requirements.txt                        # 依赖列表
└── README.md                               # 项目说明

```

## 核心功能模块

### 1. 统一数据库模块 (src/db/)
- **文件**: `database.py`
- **功能**:
  - 连接PostgreSQL数据库 (127.0.0.1)
  - 从agent_audits表读取审计结果
  - 获取论文内容用于证据验证
  - 保存反思评估结果
- **配置**:
  - 主机: 127.0.0.1
  - 用户名: admin
  - 密码: <通过环境变量设置>

### 2. 统一API模块 (src/api/)
- **文件**: `deepseek_client.py`
- **功能**:
  - 统一使用DeepSeek API进行LLM调用
  - 支持冲突裁决和对话生成
  - 自动处理API请求和错误

### 3. 冲突裁决模块 (src/conflict_resolution/)
- **负责人**: 成员A
- **功能**:
  - 检测Agent评审结果之间的冲突
  - 使用LLM进行冲突裁决
  - 计算加权平均分
  - 生成Markdown评审报告
  - 证据真实性验证

### 4. 重复过滤模块 (src/deduplication/)
- **负责人**: 成员B
- **功能**:
  - 使用DBSCAN聚类进行重复过滤
  - 识别相似的审计意见
  - 去除冗余信息

### 5. 证据验证模块 (src/evidence_validation/)
- **负责人**: 成员B
- **功能**:
  - 精确匹配验证证据引用
  - 语义匹配验证（使用Sentence-Transformers）
  - 幻觉过滤：检测虚假证据

### 6. 导师对话生成模块 (src/dialogue_generation/)
- **负责人**: 成员C
- **功能**:
  - 根据领域和问题严重性构建导师人设
  - 生成温和专业的指导性对话
  - 对话质量自动评分

### 7. 优先级排序模块 (src/priority_sorting/)
- **负责人**: 成员D
- **功能**:
  - 基于规则引擎的优先级排序
  - 检测置信度低、证据缺失等触发条件
  - 检测多组Agent冲突
  - 生成人工复核标记

## 主运行程序 (run.py)

### 支持两种运行方案

#### 方案1: 从数据库读取
```bash
# 交互式选择
python run.py

# 命令行指定
python run.py --mode database

# 处理指定论文
python run.py --mode database --paper-id paper_001
```

从PostgreSQL数据库的`agent_audits`表读取数据：
- 表结构: `(paper_id, group_id, result_json, created_at)`
- 同一paper_id的5个group_id (2-6) 对应5个审计组
- result_json格式见docs/work_week2.txt

#### 方案2: 从文件读取
```bash
# 交互式选择
python run.py

# 命令行指定
python run.py --mode file --prompts-dir prompts
```

从prompts文件夹读取JSON文件：
- 文件命名: `{paper_id}_group_{group_id}.json`
- 每篇论文需要5个JSON文件（group_id: 2-6）

### 数据流程

```
审计结果输入 (数据库/文件)
    ↓
[成员D] 优先级排序 + 复核标记
    ↓
[成员A] 冲突裁决 + 整体评分
    ↓
[成员B] 证据验证 + 去重过滤
    ↓
[成员C] 导师对话生成
    ↓
最终评审报告 (保存到数据库/文件)
```

## 测试数据生成

### 生成测试数据
```bash
# 生成3篇论文的测试数据（默认）
python tests/generate_test_data.py

# 生成10篇论文的测试数据
python tests/generate_test_data.py --num-papers 10

# 指定输出目录
python tests/generate_test_data.py --output-dir test_data
```

生成的数据包括：
- 5个审计组 (格式、逻辑、代码、实验、文献)
- 每个审计组2-5个审核项
- 随机的问题级别 (Critical/Warning/Info)
- 随机的评分 (40-95)
- 模拟的证据引用

## 环境配置

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 配置环境变量
```bash
# DeepSeek API密钥
export DEEPSEEK_API_KEY="your_api_key_here"

# 或在.env文件中配置
echo "DEEPSEEK_API_KEY=your_api_key_here" > .env
```

### 3. 数据库配置
数据库配置已硬编码在`src/db/database.py`中：
- 主机: 127.0.0.1
- 端口: 5432
- 用户名: admin
- 密码: <通过环境变量设置>
- 数据库: paper_review

## 使用示例

### 完整流程示例

```python
# 1. 生成测试数据
python tests/generate_test_data.py --num-papers 3

# 2. 运行主程序（交互式）
python run.py
# 选择: 2 (从文件读取)
# 输入目录: prompts

# 3. 查看结果
# 结果保存在 prompts/result_{paper_id}.json
```

### 从数据库运行示例

```python
# 1. 确保数据库中有数据
# agent_audits表应包含paper_id和result_json

# 2. 运行主程序
python run.py --mode database

# 3. 结果自动保存到reflection_results表
```

## 数据格式

### 输入格式 (agent_audits表 / JSON文件)

```json
{
  "group_id": 2,
  "group_name": "格式审计组",
  "paper_id": "paper_001",
  "audit_results": [
    {
      "id": "item-001",
      "point": "标题层级规范",
      "score": 85,
      "level": "Warning",
      "description": "标题层级存在问题",
      "evidence_quote": "原文第2.1节提到：...",
      "location": {"section": "2.1", "line_start": 45},
      "suggestion": "建议调整标题层级"
    }
  ],
  "timestamp": "2026-03-05T20:00:00"
}
```

### 输出格式 (reflection_results表 / result_{paper_id}.json)

```json
{
  "paper_id": "paper_001",
  "final_score": 82.5,
  "verdict": "论文质量良好，建议小修后录用",
  "critical_issues": [],
  "major_issues": [...],
  "minor_issues": [...],
  "needs_human_review": false,
  "human_review_reason": null,
  "mentor_dialogue": {...},
  "metadata": {
    "conflict_resolution": {...},
    "review_marks_count": 0,
    "sorted_results_count": 15
  }
}
```

## 技术栈

- **框架**: FastAPI, Pydantic
- **LLM**: DeepSeek API
- **NLP**: Sentence-Transformers, scikit-learn
- **数据库**: asyncpg (PostgreSQL)
- **HTTP**: httpx
- **测试**: pytest

## 错误处理

项目包含完善的错误处理机制：

1. **数据库连接错误**: 自动重试和日志记录
2. **API调用失败**: 捕获HTTP错误并记录详细信息
3. **数据格式错误**: 验证输入数据格式，跳过无效数据
4. **模块初始化失败**: 记录错误并优雅退出
5. **文件读写错误**: 捕获IO异常并提示用户

## 注意事项

1. **API密钥**: 必须配置DEEPSEEK_API_KEY环境变量
2. **数据库连接**: 确保能访问127.0.0.1:5432
3. **数据格式**: 输入JSON必须符合work_week2.txt中的格式
4. **审计组数量**: 每篇论文必须有5个审计组的结果（group_id: 2-6）
5. **证据引用**: 所有Warning/Critical级别的问题必须包含evidence_quote

## 后续优化建议

1. **配置文件**: 将数据库配置和API配置移到配置文件
2. **异步优化**: 并行处理多篇论文以提高效率
3. **缓存机制**: 缓存LLM调用结果以减少API开销
4. **Web界面**: 开发Web前端展示评审结果
5. **监控告警**: 添加系统监控和异常告警

## 贡献者

- 成员A: 冲突裁决和整体评分
- 成员B: 重复过滤和幻觉过滤
- 成员C: 导师对话生成
- 成员D: 优先级排序和系统集成
